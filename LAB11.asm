;630510619

.MODEL	SMALL
.STACK	100H
.CODE

MAIN	PROC
;DISPLAY PROMPT
	MOV	AH, 2 		
	MOV	DL, '?'	
	INT	21H
;GET NUMBER AND PLUS NUMBER		
	MOV AH, 1 
	INT 21H
	MOV BL, AL
	INT 21H
	ADD BL,AL
	SUB BL,30H
;NEW LINE	
	MOV AH, 2
	MOV DL,0DH
	INT 21H
	MOV DL,0AH
	INT 21H
;DISPLAY SUM
	MOV DL, BL
	INT 21H
;RETURN RESULT
	MOV AH,4CH 
	INT 21H 
MAIN	ENDP
	END MAIN
	;AH AL DL